# again, instead of making our own code, we can use someone elses code.

from collections import Counter
list1=['apple','egg','apple','banana','egg','apple']
counts = Counter(list1)
print(counts)

# lets see how we can use this tool to help us